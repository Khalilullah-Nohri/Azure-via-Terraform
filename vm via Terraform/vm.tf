resource "azurerm_linux_virtual_machine" "vm" {
  name                = "13Sept"
  resource_group_name = azurerm_resource_group.rg.name
  location            = var.location
  size                = var.vm_size
  admin_username      = var.admin_username
  admin_password      = var.admin_password
  disable_password_authentication = false   # 👈 Add this: When an admin_password is specified disable_password_authentication must be set to false. ~> Note: One of either admin_password or admin_ssh_key must be specified.


  network_interface_ids = [
    azurerm_network_interface.nic.id
  ]

  os_disk {
    caching              = "ReadWrite"
    storage_account_type = "Standard_LRS"
  }

  source_image_reference {
    publisher = "Canonical"
    offer     = "UbuntuServer"
    sku       = "18.04-LTS"
    version   = "latest"
  }
}
